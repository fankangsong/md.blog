#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-sitemap/bin/grunt-sitemap" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-sitemap/bin/grunt-sitemap" "$@"
  ret=$?
fi
exit $ret
